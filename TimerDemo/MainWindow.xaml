<Window
    x:Class="TimerDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TimerDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=WindowTitel, FallbackValue=~WindowTitel}"
    Width="900"
    Height="600"
    mc:Ignorable="d">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>
    <Grid x:Name="gridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="70" />
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <Button
                x:Name="BtnCloseApplication"
                Width="30"
                Height="30"
                HorizontalAlignment="Left">
                <Image Source="{StaticResource ResourceKey=IconApplicationExit2}" Stretch="None" />
            </Button>

            <Button
                x:Name="BtnDispatcherTimer"
                Width="130"
                Height="30"
                Margin="5,0,0,0"
                HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <Image Source="{StaticResource ResourceKey=IconTimer}" Stretch="None" />
                    <TextBlock
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        FontWeight="Medium"
                        Text="DispatcherTimer" />
                </StackPanel>
            </Button>
        </StackPanel>

        <Rectangle
            Grid.Row="0"
            Height="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Fill="Black" />

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Text="Timervarianten unter NET 10" />

        <Rectangle
            Grid.Row="1"
            Height="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Fill="Black" />

        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            Margin="10,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            FontSize="24">
            <Run Text="Timervarianten" />
            <Run Text=":" />
            <Run Text="{Binding Path=Timervarianten}" />
        </TextBlock>

        <Rectangle
            Grid.Row="2"
            Height="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Fill="Blue" />

        <TextBlock
            Grid.Row="3"
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Text="{Binding Path=TimerResult}" />

    </Grid>
</Window>
